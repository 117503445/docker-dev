- 文档、注释 使用中文。每个方法需要有注释，代码块内在必要的地方添加注释。写文档尽量保持精简。
- 如果通过 `go-task smoke` 命令可以运行冒烟测试，且需要修改代码，则
    1. 在测试用例中覆盖新增的需求。运行 `go-task smoke`，预期会因为新增用例失败。
    2. 修改代码，运行 `go-task smoke`，直到成功。密钥文件必须放在 .env 中，并且在代码中主动加载 .env。不应该放在 vcs 的文件（如代码生成的）应使用 .gitignore 排除。