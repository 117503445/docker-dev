# 添加 ANTHROPIC_AUTH_TOKEN 环境变量说明

## 主要内容和目的

在 README.md 中添加 `ANTHROPIC_AUTH_TOKEN` 环境变量的文档说明，方便用户配置 Anthropic API 认证令牌。

## 更改内容描述

1. 在「运行时可配置环境变量」表格中新增 `ANTHROPIC_AUTH_TOKEN` 环境变量说明
2. 在 `compose.override.yaml` 示例中添加 `ANTHROPIC_AUTH_TOKEN` 的使用示例

## 验证方法和结果

- 查看 README.md 确认环境变量表格和示例均已正确添加